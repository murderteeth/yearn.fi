/**
 * Generic Response helpers now live in `@yearn/components`, shared by this app
 * and by the route handlers `@yearn/deposit` ships. Re-exported here to keep
 * the existing `@/server/http` import path working.
 */

export * from '@yearn/components/server/http'
